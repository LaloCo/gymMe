<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:gymMe" x:Class="gymMe.MainPage">
    
    <StackLayout Margin="5"
                 VerticalOptions="Center">
        <!-- Place new controls here -->
        <Entry x:Name="emailEntry"
               Placeholder="Email"
               Keyboard="Email" />
        <Entry x:Name="passwordEntry"
               Placeholder="Password"
               IsPassword="true" />
        <Button x:Name="loginButton"
                Margin="0, 50, 0, 0"
                Clicked="loginButton_Clicked"
                Text="LOG IN"
                Style="{StaticResource loginButton}"/>
    </StackLayout>
</ContentPage>